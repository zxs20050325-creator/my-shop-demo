services:
  - type: web
    name: vue-frontend
    env: static
    buildCommand: npm install && npm run render-build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_ENV
        value: production